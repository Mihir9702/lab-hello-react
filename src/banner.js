import { HeaderText, HeaderImg } from "./headerComponents";

const Banner = () => {
  return (
    <header className="banner">
      <HeaderImg />
      <HeaderText />
    </header>
  );
};

export default Banner;
